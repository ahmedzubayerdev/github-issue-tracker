### 1 What is the difference between var, let, and const? 

the all are used for diclareing variables in javaScript.
variables declared through  var and let can be reassigned but const cannot be reassignable . 
var is hoisted  but let and const are not hoisted. 

### 2 What is the spread operator (...)? 
 it is a operator in javascript that expand array, object or iterable ekements.

### 3 What is the difference between map(), filter(), and forEach()? 

those are methods used for looping an array .
the map method loop through each array element and perform some task and then return a new array .

the filter method also loop through each element and perform a condition check and if satisfied the condition push into a new array and finally returns the new array.

forEach method execute a function for each element of an array but it doesn't return anything.

### 4 What is an arrow function? 

Arrow functions are a shorter syntax for writing functions introduced in ES6.

### 5 What are template literals?

templete literals is a technique to write string where someone can write single line string , multi-line string or even variables and expression . 